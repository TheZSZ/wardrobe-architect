events {
    worker_connections 1024;
}

http {
    # Image cache configuration
    proxy_cache_path /var/cache/nginx/images levels=1:2 keys_zone=images_cache:10m max_size=1g inactive=7d;

    upstream api {
        server wardrobe-api:8000;
    }

    server {
        listen 80;
        server_name _;

        # Image endpoints - cached for 7 days
        location /images/ {
            proxy_pass http://api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Cache settings
            proxy_cache images_cache;
            proxy_cache_key "$scheme$proxy_host$uri";   # Ignore api_key query param
            proxy_cache_valid 200 7d;
            proxy_cache_valid 301 302 1h;
            proxy_cache_valid 401 403 404 0;            # Never cache errors
            proxy_cache_valid any 0;

            # Prevent stampedes
            proxy_cache_lock on;
            proxy_cache_lock_timeout 10s;
            proxy_cache_revalidate on;
            proxy_cache_use_stale error timeout updating;

            add_header X-Cache-Status $upstream_cache_status always;
        }

        # API and web UI - no caching
        location / {
            proxy_pass http://api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Image upload size limit
        client_max_body_size 20M;
    }
}
